<%= form_with(model: @menu, local: true) do |form| %>
  <% if @menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@menu.errors.count, "error") %> prohibited this @menu from being saved:</h2>

      <ul>
      <% @menu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :menu_name, 'メニュー名' %>
    <%= form.text_field :menu_name %>
  </div>

  <div class="field">
    <%= form.label :time_require %>
    <%= form.number_field :time_require %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :menu_content %>
    <%= form.text_field :menu_content %>
  </div>

  <%= form.hidden_field :staff_id, :value => current_user.staff_ids %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
