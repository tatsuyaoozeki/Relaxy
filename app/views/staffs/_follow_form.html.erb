<% unless current_user == @user %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'staffs/unfollow' %>
  <% else %>
    <%= render 'staffs/follow' %>
  <% end %>
  </div>
<% end %>
